# The API base URL of the FastAPI backend.
# For local frontend development, use the port your backend is running on (default 8000).
VITE_API_BASE_URL=http://localhost:8000/api

# For production deployment, set this in Vercel to your deployed backend URL:
# VITE_API_BASE_URL=https://your-backend-service.onrender.com/api